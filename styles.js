import { StyleSheet } from 'react-native';

export const MainStyle = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
    },
    dishSearch: {
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
        
    }
})
